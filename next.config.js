/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images : {
        domains : ['images.unsplash.com', 'bucketforfahim.s3.us-east-2.amazonaws.com', "nix-tag-images.s3.amazonaws.com"]
    },
}
